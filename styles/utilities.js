import css from 'next/css';

export const SR_ONLY = css({
  border: 0,
  clip: 'rect(0 0 0 0)',
  height: '1px',
  margin: '-1px',
  overflow: 'hidden',
  padding: 0,
  position: 'absolute',
  width: '1px',
  transform: 'translatez(0)',
});

export default SR_ONLY;
